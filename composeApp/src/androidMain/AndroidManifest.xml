<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!--    as suggested by (for android 11 compat)-->
<!-- https://forums.garmin.com/developer/connect-iq/f/discussion/262512/android-sdk-won-t-work-with-android-11-->
<!--    <queries>-->
<!--        <package android:name="com.garmin.android.apps.connectmobile"/>-->
<!--        <package android:name="com.garmin.android.connectiq"/>-->
<!--    </queries>-->

    <application
        android:allowBackup="true"
        android:icon="${appIcon}"
        android:label="@string/app_name"
        android:roundIcon="${appIconRound}"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        android:largeHeap="true"
        >
        <activity
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter android:label="Strava Callback">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Changed to match Strava Dashboard's 'localhost' -->
                <data android:scheme="paulapp" android:host="localhost" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/vnd.ant.fit" />
                <data android:mimeType="application/fit" />
            </intent-filter>

            <!-- Intent filter to VIEW GPX/FIT files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/vnd.ant.fit" />
                <data android:mimeType="application/fit" />
                <data android:mimeType="application/octet-stream" />
                <data android:host="*" />
            </intent-filter>

            <!-- fit files are extremely hard to handle for some reason (samsung file manager), so just accept all "view file"-->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
<!--                <data android:mimeType="*/*" />-->
<!--                even octet stream does not match it :(-->
<!--                <data android:mimeType="application/octet-stream" />-->
<!--                <data android:host="*" />-->
                <!-- i cant even add the below, or i get 'there are no apps to open this file type' when opening a fit file-->
                <!-- <data android:pathPattern=".*fit" /> -->
<!--                -->
<!--                val uri = intent.data-->

<!--                // 1. Check what the Intent claims the MIME type is-->
<!--                val intentMimeType = intent.type-->
<!--                println("DEBUG: Intent MIME Type is: '$intentMimeType'")-->
<!--                prints ""-->

<!--                // 2. Ask the ContentProvider what it thinks the type is-->
<!--                val actualMimeType = uri?.let { contentResolver.getType(it) }-->
<!--                println("DEBUG: ContentResolver MIME Type is: '$actualMimeType'")-->
<!--                prints "application/octet-stream"-->
<!--                When an app uses a MIME Group, Android forces the package manager to actively query the ContentResolver to resolve the type before deciding whether to show your app in the menu.-->
<!--                so use mimeGroup instead of mimeType="*/*"-->
<!--                requires opening app at least once to register the mime group -->
<!--                this should let us control the registration of the group dynamically, adding and removing the group if the user wants to turn off the filter-->
                <data android:mimeGroup="fit_files" />
            </intent-filter>

            <!-- this is needed for apps share to (opentopo maps etc.), it doesn't provide extension, so you can't add path  -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.OPENABLE" />

                <data
                    android:mimeType="*/*"
                    android:scheme="content" />
            </intent-filter>
<!--             for google maps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <service android:name=".WebServerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local server for Garmin device communication" />
        </service>
        <receiver android:name=".ConnectIQMessageReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.garmin.android.connectiq.INCOMING_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>